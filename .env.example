# Forge Development Environment Variables
# Copy this file to .env and fill in the values
#
# This file is used by the `just` commands to configure the local
# development environment. For component-specific configuration, see:
#   - platform/.env.example    (platform server configuration)
#   - agent/claudecode/.env.example (agent configuration)

# =============================================================================
# Required for Development
# =============================================================================

# Anthropic API key (required - used to create K8s secret for agents)
ANTHROPIC_API_KEY=

# =============================================================================
# Database Configuration (for goose migrations)
# =============================================================================

# Development database URL (used with --env=dev or by default)
DATABASE_URL_DEV=postgres://forge:forge@localhost:5432/forge_dev?sslmode=disable

# Production database URL (used with --env=prod)
DATABASE_URL_PROD=

# =============================================================================
# Local Postgres (Docker)
# =============================================================================

# Local postgres container configuration
POSTGRES_PORT=5432
POSTGRES_USER=forge
POSTGRES_PASSWORD=forge
POSTGRES_DB=forge_dev

# =============================================================================
# Optional Overrides
# =============================================================================

# K3d cluster name
# CLUSTER_NAME=forge-dev

# K3d registry configuration
# K3D_REGISTRY_NAME=forge-registry.localhost
# K3D_REGISTRY_PORT=5111

# Kubernetes namespace for agent pods
# AGENT_NAMESPACE=default

# Agent image configuration (for local registry)
# AGENT_IMAGE_NAME=forge-agent
# AGENT_IMAGE_TAG=latest
